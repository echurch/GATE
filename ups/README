To declare GATE as an ups product, type:

ups declare GATE <version> -r $GATEDIR 
  -f ANY  -U $GATEDIR/ups/ -m GATE.table -M $GATEDIR/ups/

where GATEDIR is the path to the the root directory of GATE
